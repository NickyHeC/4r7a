# Commits listed here are ignored by `git blame` when blame.ignoreRevsFile is set.
# Configure once per clone: git config blame.ignoreRevsFile .git-blame-ignore-revs

# One-time ruff format adoption (2026-07-02)
5d3f017
